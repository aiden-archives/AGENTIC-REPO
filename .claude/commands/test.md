# Run Tests

Run the test suite for all services.

## Steps
1. Run `make test` to execute all tests
2. Report any failures with file locations
3. Suggest fixes for failing tests

## Options
- Run specific service tests: `make test SVC=api` or `make test SVC=web`
- Run with coverage: `make test-coverage`
