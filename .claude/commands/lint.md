# Lint Code

Run linters across the codebase.

## Steps
1. Run `make lint` to check all code
2. Report any linting errors with file locations
3. Offer to auto-fix with `make lint-fix`

## Linters
- ESLint for JavaScript/TypeScript
- Prettier for formatting
- Hadolint for Dockerfiles
